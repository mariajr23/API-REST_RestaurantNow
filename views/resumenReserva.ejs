<!DOCTYPE html>
<html lang="es">
  <head>
    <link
      href="/node_modules/bootstrap/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script src="/node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, user-scalable=no, initial-scale=1.0, minimum-scale=1.0"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h3>Resumen de tu Reserva</h3>
      <p>Restaurante: <%= restaurant %></p>
      <p>Fecha: <%= fecha %></p>
      <p>Hora: <%= hora %></p>

      <h4>Platos Seleccionados:</h4>
      <ul>
        <% if (platos && platos.length > 0) { %> <% platos.forEach(plato => { %>
        <li>
          <%= plato.cantidad %> x <%= plato.nombre %> - Precio: $<%=
          plato.precio %>
        </li>
        <% }) %> <% } else { %>
        <li>No hay platos seleccionados.</li>
        <% } %>
      </ul>
      <h4>Precio Total: $<%= total %></h4>

      <form action="/realizarPago" method="POST">
        <input type="hidden" name="fecha" value="<%= fecha %>" />
        <input type="hidden" name="hora" value="<%= hora %>" />
        <input
          type="hidden"
          name="id_restaurante"
          value="<%= id_restaurante %>"
        />
        <input
          type="hidden"
          name="platosSeleccionados"
          value="<%= JSON.stringify(platos) %>"
        />

        <!-- Aquí podrías mostrar el formulario de pago con algún método que quieras utilizar -->
        <button type="submit" class="btn btn-primary">Realizar Pago</button>
      </form>
    </div>
  </body>
</html>
